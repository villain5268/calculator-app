const convertToKilometers = (miles) => {
    return miles * 1.60934;
  };
  
  const convertToMiles = (kilometers) => {
    return kilometers / 1.60934;
  };
  
  export { convertToKilometers, convertToMiles };
  